#!/usr/bin/env groovy

@Library('shared-library@master') _

pipeline {
  agent any

  stages {
    stage ('maven build') {
      steps {
        script {
          sh 'echo mv clean install'
        }
      }
    }
    stage ('check logs') {
      steps {
        filterLogs ( 'WARNING', 1)
      }
    }
  }
}
